http {
    server {
        listen 8080;

        location / {
            proxy_pass http://client:3000;
        }

        location /api {
            rewrite ^/api/(.*) /$1 break;
            proxy_pass http://server:5000;
        }
    }
}